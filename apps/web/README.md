# Web

LongTeng 个人网站前端，基于 React、TypeScript 与 Vite。

## 常用命令

```bash
npm install
npm run dev
npm run build
npm run preview
```

页面入口和业务组件位于 `src/`，静态资源位于 `public/`。生产部署统一从仓库根目录的 `scripts/deploy/` 执行。
