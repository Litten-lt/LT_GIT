{
  "name": "longteng-personal-site",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace chesshub-site",
    "dev:api": "npm run dev --workspace chesshub-server",
    "build": "npm run build --workspace chesshub-site",
    "test": "npm run test --workspace chesshub-server",
    "verify": "npm run build && npm run test"
  }
}
