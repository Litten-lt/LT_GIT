import React from 'react'
import ReactDOM from 'react-dom/client'
import UnifiedComposer from './pages/UnifiedComposer'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><UnifiedComposer /></React.StrictMode>)
